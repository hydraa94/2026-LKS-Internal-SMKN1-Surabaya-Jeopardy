services:
  no-cat:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: misc-no-cat

    hostname: no-cat

    environment:
      - TERM=xterm

    ports:
      - "1347:1337"

    restart: unless-stopped